# FreeAssetFilter 依赖列表
# 生成时间: 2026-02-12
# 项目地址: https://github.com/Dorufoc/FreeAssetFilter
# Python版本要求: >=3.8

# 必须安装的依赖，缺少这些应用将无法正常运行

# GUI框架 - PySide6 (Qt6的Python绑定)
# PySide6 6.4+ 支持 Python 3.8+
PySide6>=6.4.0
PySide6-Addons>=6.4.0
PySide6-Essentials>=6.4.0
shiboken6>=6.4.0

# 图像处理库
# Pillow 9.0+ 支持 Python 3.6+
Pillow>=9.0.0
pillow-heif>=0.10.0
imageio>=2.15.0
aggdraw>=1.3.15

# PSD文件支持 (用于Adobe Photoshop文件预览)
# psd-tools 1.9+ 支持 Python 3.8+
psd-tools>=1.9.0

# PDF处理
# PyMuPDF 1.20+ 支持 Python 3.8+
PyMuPDF>=1.20.0

# RAW图像处理 (用于相机RAW格式支持)
# rawpy 0.17+ 支持 Python 3.8+
rawpy>=0.17.0

# OpenCV (用于视频缩略图生成)
# opencv-python 4.5+ 支持 Python 3.8+
opencv-python>=4.5.0

# 音频元数据处理
# mutagen 1.45+ 支持 Python 3.6+
mutagen>=1.45.0

# 压缩文件支持
rarfile>=4.0
py7zr>=0.20.0
pycdlib>=1.0.0
multivolumefile>=0.2.0
pybcj>=0.5.0
pyppmd>=0.18.0
pyzstd>=0.15.0
inflate64>=0.3.0
brotli>=1.0.0
pycryptodomex>=3.15.0
texttable>=1.6.0

# 字符编码检测
chardet>=4.0.0

# 版本解析 (用于依赖检查)
packaging>=20.0

# 语法高亮
Pygments>=2.12.0

# Markdown渲染
Markdown>=3.4.0

# EXIF信息读取
ExifRead>=2.3.0

# 科学计算
# numpy 1.21+ 支持 Python 3.8+
numpy>=1.21.0
# scipy 1.7+ 支持 Python 3.8+
scipy>=1.7.0
# scikit-image 0.19+ 支持 Python 3.8+
scikit-image>=0.19.0
networkx>=2.6.0
tifffile>=2021.0.0

# 系统工具
psutil>=5.8.0

# 其他依赖
attrs>=21.0.0
lazy_loader>=0.1.0
importlib_metadata>=4.0.0
zipp>=3.5.0
typing_extensions>=4.0.0
